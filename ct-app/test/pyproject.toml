[project]
name = "test"
version = "3.11.2"
requires-python = ">=3.13"
dependencies = [
    "pytest==8.3.5",
    "pytest-asyncio==0.26.0",
    "pytest-mock==3.14.0",
]

[tool.setuptools]
packages = ["api", "components", "rpc", "subgraph"]