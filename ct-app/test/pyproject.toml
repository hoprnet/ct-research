[project]
name = "test"
version = "3.11.2"
requires-python = ">=3.14"
dependencies = [
    "pytest>=8.3.5",
    "pytest-asyncio>=1.0.0",
    "pytest-mock>=3.14.1",
]

[tool.setuptools]
packages = ["api", "components", "subgraph"]