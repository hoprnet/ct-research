[project]
name = "core-mixins"
version = "1.0.0"
requires-python = ">=3.14"
dependencies = ["prometheus_client>=0.22.0"]

[tool.setuptools]
packages = ["core.mixins"]
package-dir = {"core.mixins" = "."}
