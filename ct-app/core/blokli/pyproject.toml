[project]
name = "core-blokli"
version = "1.0.0"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.13.4",
    "api-lib>=0.0.4",
    "multidict>=6.4.3",
    "prometheus_client>=0.22.0"
]

[tool.setuptools]
packages = ["core.blokli"]
package-dir = {"core.blokli" = "."}
