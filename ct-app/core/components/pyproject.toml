[project]
name = "core-components"
version = "1.1.0"
requires-python = ">=3.14"
dependencies = [
    "prometheus_client>=0.22.0",
]

[tool.setuptools]
packages = ["core.components"]
package-dir = {"core.components" = "."}