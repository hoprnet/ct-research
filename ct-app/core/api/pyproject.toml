[project]
name = "core-api"
version = "1.2.0"
requires-python = ">=3.14"
dependencies = [
    "api-lib>=0.0.4",
    "prometheus_client>=0.22.0",
]

[tool.setuptools]
packages = ["core.api"]
package-dir = {"core.api" = "."}
